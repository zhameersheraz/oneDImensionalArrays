# IntegerArray

Demonstrates a hard-coded integer array. The program does not read any input; it just prints the ten preset values.

## Compile & Run

```bash
javac IntegerArray.java
java IntegerArray
```

See [docs/integer-array.md](../docs/integer-array.md) for the full description and sample run.